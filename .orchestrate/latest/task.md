# Task

Bootstrap `algo-trader` from current state to `/ak-ship` pipeline using `--auto --parallel` mode, equivalent to running `/mk-bootstrap` then `/ak-ship --auto --parallel` inside this project.

Source package: algo-trader at `/Users/macbook/algo-trader`
