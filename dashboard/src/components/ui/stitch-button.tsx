/**
 * StitchButton — design-system button wrapper.
 */
import { Button } from './button';
import type { ButtonProps } from './button';

export const StitchButton = Button;
export type StitchButtonProps = ButtonProps;
