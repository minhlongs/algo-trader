/**
 * Shared Backtesting — Barrel Export
 *
 * Portfolio-agnostic metrics computation from trade history.
 * For strategy-level simulation, see src/desk/backtesting/.
 */
export { BacktestRunner } from './backtest-runner';
export type { BacktestTrade, BacktestConfig, BacktestResult } from './backtest-runner';
