/**
 * STUB — not implemented. Not imported anywhere; superseded by src/desk/commands/arb-auto.ts
 * which is the version wired into src/index.ts.
 * This file can be deleted once confirmed unused by any dynamic imports.
 */
export function runArbAuto(_options?: { symbols?: string; exchanges?: string; minSpread?: number; dryRun?: boolean; verbose?: boolean }): void {
  throw new Error(
    'arb-auto stub — use src/desk/commands/arb-auto.ts which is wired into the CLI',
  );
}
