/**
 * Paper Exchange — V2 migration compatibility stub.
 *
 * Simulates exchange matching for paper trading mode.
 * NOTE: Placeholder for the paper trading simulation engine.
 */
import { logger } from '../desk/core/logger';

export class PaperExchange {
  constructor() {
    logger.debug('PaperExchange stub created', 'PaperExchange');
  }
}
