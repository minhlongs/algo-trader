# Brainstorm — Phase A (Billing Polish) + Phase B (MCP Server) **Plan ID:** 260708-0255 | **Date:** 2026-07-08 | **Input:** `/brainstorm /goal /idea next` after Signals API v2 (260708-0000) shipped ## Context Signals API v2 shipped (plan 260708-0000): 8 endpoints, OpenAPI 3.1 spec, D1 migration, usage metering wired, dogfood endpoint created. Ideation plan (260705-0025) scored 21/30 GO. Top P0 remain: billing layer half-wired, MCP moat not started. ## Problem Statement 1. **Revenue hygiene:** `/usage/:subscriberId` returns 501 stub, dead `signNOWPaymentsWebhook`/`verifyNOWPaymentsSignature` in `payment-service.ts`, 6+ docs reference stale $49 PRO price (code has $99), Telegram bot holds `/link` data in in-memory Map (lost on restart). 2. **Differentiation (moat):** Zero MCP infrastructure. AI agents can't natively discover or subscribe to signals via Model Context Protocol. Ideation identified agent-native positioning as the competitive moat. ## Approaches Evaluated | Path | Deliverable | Effort | Risk | |------|-------------|--------|------| | A — Billing polish | Fix stub, kill dead code, unify pricing, D1 Telegram | 2–3 days | Low | | B — MCP server | Agent-native signal discovery via stdio server | 1–2 weeks | Medium | | Both in sequence | Full hygiene → moat | 3–4 weeks | Managed | **Discarded:** Provider registry (supply-side) — premature, no providers onboarded. x402 micropayments — Phase 3 per ideation, out of scope. ## Decision: A then B, both in one plan Two-phase plan: ### Phase A — Billing Polish + Hygiene A1. Wire `/usage/:subscriberId` stub → real `usageMetering.getSnapshot()` A2. Delete dead payment functions (`signNOWPaymentsWebhook`, `verifyNOWPaymentsSignature`) A3. Telegram `/link` D1 migration — replace in-memory `userSessions` Map with `userSessionRepo` A4. Pricing doc cleanup — $49 → $99 across 6 doc files ### Phase B — MCP Server for Signal Discovery B1. Scaffold stdio MCP server (`@modelcontextprotocol/sdk`) B2. `list_tools`: `get_signals(tier, since, limit)`, `get_subscription_status()` B3. `list_resources`: signal feed as paginated resources B4. Reuse existing `signal-tier-resolver` for auth/tier enforcement B5. Tests for tool contracts, auth rejection, tier filtering B6. Doc update in `api-reference-v2.md` ## Constraints - Cluster 1 only (platform/API layer), no DB schema changes - No icon library additions - YAGNI: skip x402, provider registry, hash commitments ## Files Touched (A) `src/platform/api/routes/signal-subscription-routes.ts` `src/platform/billing/payment-service.ts` `src/platform/telegram/bot.ts` `src/platform/telegram/bot-command-handlers.ts` `docs/system-architecture.md`, `docs/api-rate-limiting.md`, `docs/project-changelog.md`, `docs/trading-architecture-sops.md` ## Files Touched (B) `src/platform/mcp/signal-mcp-server.ts` (new) `tests/unit/signal-mcp-server.test.ts` (new) `src/platform/middleware/signal-tier-resolver.ts` `docs/api-reference-v2.md` ## Acceptance Criteria A: tests pass, `/usage/:subscriberId` returns real data, Telegram `/link` persists, zero dead payment functions B: agent connects via MCP client, calls `get_signals`, receives tier-filtered results ## Risks - **A3 Telegram migration:** if `bot.ts` has more in-memory state than the link Map, migration scope grows — verify before starting - **B MCP SDK:** `@modelcontextprotocol/sdk` not in package.json — need to add dep → scope creep if version mismatch ## Unresolved Questions - None — scoped and ready to plan. 