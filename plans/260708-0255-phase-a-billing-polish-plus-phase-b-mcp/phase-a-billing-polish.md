# Phase A — Billing Polish + Hygiene **Priority:** P0 | **Effort:** 2–3 days **Inputs:** brainstorm report, `src/platform/api/routes/signal-subscription-routes.ts`, `src/platform/billing/payment-service.ts`, `src/platform/telegram/bot.ts`, `src/platform/telegram/bot-command-handlers.ts` ### Overview Fix the billing layer that was left half-wired after v2 ship: 1. Unblock the `/usage/:subscriberId` stub (returns 501 today) 2. Remove dead Polar/NOWPayments functions left from rejected integration 3. Migrate Telegram bot user sessions from in-memory Map to D1 4. Unify stale pricing docs ($49 → $99 PRO) --- ## Files to Modify 1. `src/platform/api/routes/signal-subscription-routes.ts` — wire `/usage/:subscriberId` stub 2. `src/platform/billing/payment-service.ts` — delete dead functions 3. `src/platform/telegram/bot.ts` — replace in-memory `userSessions` Map with `userSessionRepo` 4. `src/platform/telegram/bot-command-handlers.ts` — verify already using D1 (should be no-op) 5. `docs/system-architecture.md` — $49 → $99 6. `docs/api-rate-limiting.md` — $49 → $99 7. `docs/project-changelog.md` — $49 → $99 8. `docs/trading-architecture-sops.md` — $49 → $99 9. `docs/marketing/blog-arbitrage-engine.md` — $49 → $99 10. `docs/marketing/launch-twitter-thread.md` — $49 → $99 11. `docs/marketing/investor-one-pager.md` — $49 → $99 --- ## Implementation Steps 1. **Wire `/usage/:subscriberId`:** Replace the 501 stub with a call to `usageMetering.getSnapshot(subscriberId)`. Return shape matches the internal billing endpoint: `{ subscriberId, period, callsThisPeriod, periodLimit, overage }`. Export `usageMetering` from its current location. Error handling: 400 if missing subscriberId, 404 if no snapshot, 500 on failure. 2. **Delete dead payment functions:** In `payment-service.ts`, remove `signNOWPaymentsWebhook` and `verifyNOWPaymentsSignature` plus any imports that become unused. Verify no callers remain via grep before deleting. 3. **Telegram D1 migration:** Read `bot.ts` to find all locations that read/write `this.userSessions` Map. Replace each with `userSessionRepo.getByUserId()`, `.upsert()`, or `.linkLicenseKey()` as appropriate. Ensure `userSessionRepo.ensureTable()` is called on bot startup. 4. **Pricing doc cleanup:** Grep for `\$49` and ` PRO \$49` / `PRO $49` patterns in the 6 doc files above. Replace with `$99`. Do NOT change ENTERPRISE pricing ($299 stays). --- ## Todo Checklist - [ ] Wire `/usage/:subscriberId` to `usageMetering.getSnapshot()` - [ ] Delete `signNOWPaymentsWebhook` + `verifyNOWPaymentsSignature` - [ ] Replace `bot.ts` in-memory `userSessions` Map with `userSessionRepo` calls - [ ] Call `userSessionRepo.ensureTable()` on bot startup - [ ] Update 6 doc files: $49 → $99 PRO - [ ] Run `npx vitest run` — all tests pass - [ ] Verify `/usage/:subscriberId` returns real data via curl --- ## Risk Assessment | Risk | Likelihood | Mitigation | |------|-----------|-----------| | `bot.ts` has more in-memory state than just link Map | Medium | Scout all `this.userSessions` usages before editing; if other state exists, scope grows | | Dead payment functions have indirect callers | Low | Grep entire `src/` before deleting | | Pricing docs have unrelated $49 figures (competitor pricing) | Low | Read each match in context before replacing | ## Rollback Each step is independently reversible via git. Doc changes are safe to revert. Code changes (A1-A3) can be reverted if tests fail.
