================================================================================
  PNPM DEPENDENCY SECURITY AUDIT REPORT
  Project:  /Users/macbook/algo-trader
  Generated: 2026-07-02
  Command:   pnpm audit --audit-level=high
================================================================================

TOTAL FINDINGS: 104 (6 low | 62 moderate | 36 high)
All 36 HIGH-severity vulnerabilities are analyzed below.

================================================================================
CATEGORY 1: DIRECT DEPENDENCIES (can be bumped directly in package.json)
================================================================================

1. fastify 5.7.4 -> 5.8.5+
   Advisory: GHSA-247c-9743-5963 — Body Schema Validation Bypass via Leading Space
              in Content-Type Header
   Path:     .>fastify
   Fix:      UPDATE package.json: "fastify": "^5.8.5"

2. hono 4.12.9 -> 4.12.25+
   Advisory: GHSA-88fw-hqm2-52qc — CORS Middleware reflects any Origin with
              credentials when `origin` defaults to wildcard
   Path:     .>hono
   Fix:      UPDATE package.json: "hono": "^4.12.25"

3. better-auth 1.6.3 -> 1.6.11+
   Advisory: GHSA-cq3f-vc6p-68fh — Device authorization approve/deny accepts
              any authenticated session while user code is pending
   Path:     .>better-auth
   Fix:      UPDATE package.json: "better-auth": "^1.6.11"

4. ws (root from ethers/viem/ccxt/polymarket) 8.19.0 -> 8.21.0+
   Advisory: GHSA-96hv-2xvq-fx4p — Memory exhaustion DoS from tiny fragments
              and data chunks
   Paths:    .>wrangler>miniflare>ws
             .>ethers>ws
             .>viem>ws
             .>ccxt>ws (presumed)
             .>@polymarket/clob-client>ws (presumed)
   Fix:      UPDATE package.json: "ws": "^8.21.0"
             PLUS pnpm.overrides for transitive copies (see Category 3)

================================================================================
CATEGORY 2: TRANSITIVE via @polymarket/clob-client (axios)
================================================================================

@polymarket/clob-client@5.8.0 pulls in axios@<1.16.0 (locked version unknown
but likely <1.15.0). 9 HIGH advisories affect this axios:

   GHSA-pmwg-cvhr-8vh7   — NO_PROXY bypass via RFC 1122 loopback subnet
   GHSA-pf86-5x62-jrwf   — Prototype pollution gadgets (response tampering)
   GHSA-6chq-wfr3-2hj9   — Header injection via prototype pollution
   GHSA-q8qp-cvcw-x6jj   — Credential injection / request hijacking
   GHSA-pjwm-pj3p-43mv   — IPv4-mapped IPv6 NO_PROXY bypass
   GHSA-hfxv-24rg-xrqf   — ReDoS via Cookie Name injection
   GHSA-777c-7fjr-54vf   — Resource allocation without limits
   GHSA-p92q-9vqr-4j8v   — Proxy-Authorization credential leak on redirect
   GHSA-j5f8-grm9-p9fc   — Proxy-Authorization header leak
   GHSA-3g43-6gmg-66jw   — Credential theft via prototype pollution in config
                             merge
   GHSA-35jp-ww65-95wh   — Full MitM via prototype pollution in config.proxy

Patched versions: >=1.15.1 (partial), >=1.15.2 (more), >=1.16.0 (full)

FIX: pnpm.overrides: "axios": "^1.16.0"

================================================================================
CATEGORY 3: TRANSITIVE via better-auth (kysely)
================================================================================

better-auth@1.6.3 pulls in kysely@0.28.16 which is <0.28.17.

   GHSA-pv5w-4p9q-p3v2  — JSON-path traversal injection via unsanitized
                           path-leg metacharacters in `JSONPathBuilder.key()`
                           / `.at()`

FIX OPTIONS:
   (a) UPDATE better-auth to >=1.6.11 (may pull updated kysely)
   (b) pnpm.overrides: "kysely": "^0.28.17"

================================================================================
CATEGORY 4: TRANSITIVE via @types/supertest (form-data)
================================================================================

@types/supertest@7.2.0 -> @types/superagent -> form-data@4.0.5 (<4.0.6)

   GHSA-hmw2-7cc7-3qxx  — CRLF injection in form-data via unescaped multipart
                           field names and filenames

FIX: pnpm.overrides: "form-data": "^4.0.6"

================================================================================
CATEGORY 5: TRANSITIVE via ioredis-mock (tmp)
================================================================================

ioredis-mock -> fengari -> tmp@0.2.5 (<0.2.6)

   GHSA-ph9p-34f9-6g65  — Path traversal via unsanitized prefix/postfix that
                           enables directory escape

FIX: pnpm.overrides: "tmp": "^0.2.6"

================================================================================
CATEGORY 6: TRANSITIVE via vitest (vite)
================================================================================

vitest@4.1.2 -> vite@8.0.x (locked at 8.0.3, <=8.0.15)

   3 advisories:
   GHSA-v2wj-q39q-566r   — server.fs.deny bypassed with queries
   GHSA-p9ff-h696-f583   — Arbitrary File Read via Dev Server WebSocket
   GHSA-fx2h-pf6j-xcff   — server.fs.deny bypass on Windows alternate paths

FIX: pnpm.overrides: "vite": "^8.0.16"
     (vitest works with newer vite versions)

================================================================================
CATEGORY 7: TRANSITIVE via wrangler (undici, defu)
================================================================================

7a. undici (wrangler -> miniflare)
    Locked at 5.29.0 (<6.24.0, <6.27.0)

    3 advisories:
    GHSA-vrm6-8vpv-qv8q  — Unbounded memory consumption in WebSocket
                            permessage-deflate decompression
    GHSA-v9p9-hfj2-hcw8  — Unhandled exception in WebSocket client due to
                            invalid server_max_window_bits validation
    GHSA-vxpw-j846-p89q  — WebSocket denial of service via fragment count
                            bypass

    FIX: pnpm.overrides: "undici": "^6.27.0"
    NOTE: wrangler may pin undici tightly. If override fails, update wrangler
          to the latest version (3.x line).

7b. defu (wrangler -> unenv -> defu; also better-auth -> defu)
    Locked at 6.1.4 (<=6.1.4)

    GHSA-737v-mqg7-c878  — Prototype pollution via __proto__ key in defaults
                           argument

    FIX: pnpm.overrides: "defu": "^6.1.5"

================================================================================
CATEGORY 8: TRANSITIVE via @opentelemetry (protobufjs)
================================================================================

@opentelemetry/exporter-trace-otlp-http@0.214.0 -> @opentelemetry/otlp-transformer -> protobufjs

Locked at 7.5.5 (<=7.5.5) which has 4 advisories PLUS protobufjs 7.6.0
(via different path through OTLP) has 1 more.

   GHSA-66ff-xgx4-vchm  — Code injection through bytes field in toObject
   GHSA-75px-5xx7-5xc7  — Code generation gadget after prototype pollution
   GHSA-jvwf-75h9-cwgg  — Process-wide denial of service via unsafe option
                           paths
   GHSA-685m-2w69-288q  — DoS via unbounded protobuf recursion
   GHSA-wcpc-wj8m-hjx6  — DoS via unbounded Any expansion during JSON
                           conversion (impacts 7.6.0 too)

FIX: pnpm.overrides: "protobufjs": "^7.6.1"

================================================================================
CATEGORY 9: TRANSITIVE via fastify (fast-uri)
================================================================================

fastify@5.7.4 -> @fastify/ajv-compiler -> fast-uri@3.1.0 (<=3.1.0)

   2 advisories:
   GHSA-q3j6-qgpj-74h6   — Path traversal via percent-encoded dot segments
   GHSA-v39h-62p7-jpjc   — Host confusion via percent-encoded authority
                            delimiters

FIX OPTIONS:
   (a) UPDATE fastify to 5.8.5+ (which should pull updated fast-uri)
   (b) pnpm.overrides: "fast-uri": "^3.1.2"

================================================================================
CATEGORY 10: TRANSITIVE via @aws-sdk/client-s3 (fast-xml-builder)
================================================================================

@aws-sdk/client-s3@3.1018.0 -> @aws-sdk/core -> @aws-sdk/xml-builder ->
  @aws-sdk/fast-xml-parser -> fast-xml-parser@5.5.8 -> fast-xml-builder@1.1.4

   GHSA-5wm8-gmm8-39j9  — Attribute values with unwanted quotes can bypass
                           malicious attribute injection

FIX: pnpm.overrides: "fast-xml-builder": "^1.1.7"

================================================================================
RECOMMENDED ACTIONS
================================================================================

A. BUMP DIRECTLY IN package.json (safe, no override needed):
   - fastify:  "5.7.4"   -> "^5.8.5"
   - hono:     "^4.7.0"  -> "^4.12.25"
   - ws:       "^8.19.0" -> "^8.21.0"

B. UPDATE DIRECTLY (check for breaking changes):
   - better-auth: "^1.6.3" -> "^1.6.11"
     (minor bump — review changelog for API changes)

C. REQUIRES pnpm.overrides (transitive only, not in direct deps):
   pnpm.overrides:
     "axios": "^1.16.0",
     "vite": "^8.0.16",
     "undici": "^6.27.0",
     "protobufjs": "^7.6.1",
     "defu": "^6.1.5",
     "fast-uri": "^3.1.2",
     "fast-xml-builder": "^1.1.7",
     "kysely": "^0.28.17",
     "form-data": "^4.0.6",
     "tmp": "^0.2.6"

D. UPDATE PARENT PACKAGE (alternative to overrides):
   - Update wrangler to latest 3.x to pull undici >=6.27.0
   - Update vitest to >=4.2.x to pull vite >=8.0.16
   - Update @polymarket/clob-client if a newer version bumps axios
   - Update better-auth to >=1.6.11 (already listed in B)

E. DEFER (dev-only, low risk in production):
   - form-data (via @types/supertest — only used in tests)
   - vite (via vitest — only used in test/dev)
   - tmp (via ioredis-mock — only used in tests)
   - However, to be thorough, all HIGH findings should be addressed.

================================================================================
REMEDIATION CHECKLIST
================================================================================

[ ] fastify:     5.7.4 -> 5.8.5     (direct update)
[ ] hono:        4.12.9 -> 4.12.25  (direct update)
[ ] ws:          8.19.0 -> 8.21.0   (direct update)
[ ] better-auth: 1.6.3 -> 1.6.11    (direct update, verify API compat)
[ ] axios override                  (pnpm.overrides)
[ ] vite override                   (pnpm.overrides)
[ ] undici override                 (pnpm.overrides; or update wrangler)
[ ] protobufjs override             (pnpm.overrides)
[ ] defu override                   (pnpm.overrides)
[ ] fast-uri override               (pnpm.overrides; or fastify update handles it)
[ ] fast-xml-builder override       (pnpm.overrides)
[ ] kysely override                 (pnpm.overrides; or better-auth update handles it)
[ ] form-data override              (pnpm.overrides)
[ ] tmp override                    (pnpm.overrides)
